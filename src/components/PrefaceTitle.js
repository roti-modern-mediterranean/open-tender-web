import styled from '@emotion/styled'
import { Preface } from '@open-tender/components'

const PrefaceTitle = styled(Preface)`
  font-size: 2.2rem;
  font-weight: 500;
  letter-spacing: 0.01em;
`

export default PrefaceTitle
