import styled from '@emotion/styled'

const CheckoutSubtitle = styled('div')`
  margin: 2rem 0 0;
  text-align: center;
  line-height: 1;

  p:first-of-type {
    font-weight: normal;
    font-size: 2.3rem;
    margin: 0 0 0.5rem;
  }

  p + p {
    font-weight: 500;
    font-size: 2.8rem;
    margin: 0 0 2.5rem;
  }
`

export default CheckoutSubtitle
